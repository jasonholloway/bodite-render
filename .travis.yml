sudo: required
language: Javascript
services:
  - docker
install:
  - docker build -t bbr .
  - docker create -it -v $(pwd):bb/src:ro bbr --name=bbr
  - docker start bbr
  - docker exec bbr bash -c "echo 123456"
script:
  - nunit-console ./BoditeRender.Lib.Tests/bin/Release/BoditeRender.Lib.Tests.dll
