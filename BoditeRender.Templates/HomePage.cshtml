@using RazorEngine.Templating
@using BoditeRender
@using System.Linq
@inherits BoditeTemplate<HomePage>
@{
    //var rootCat = Model.Ctx.Model.Categories.TryFind("root").Value;
    var rootCat = new { Children = Enumerable.Range(0, 50).Select(i => new { Key = i.ToString() }) };
}

<h2>HomePage: @Model.Title</h2>



<h4>@( ((IBoditeTemplate)this).Context.Model.Categories.Count )</h4>


<div>
    <h3>Categories</h3>

    @foreach(var c in rootCat.Children) {

        //need to get page from page registry...
       
        <a href="">@c.Key</a>
    }


</div>



